export default interface ProductType {
    id: number
    name: string
    code: string
    price: number
    commission:number
    total: number
    desc: string
    status: number
    created_at: string
}